import React from 'react';
import { Form } from 'antd';

const adminorder =  () => {
    return (
        <Form title="Trang chủ">
            <Form.Item>Quản lý đơn hàng</Form.Item>
        </Form>
    );
};

export default adminorder;